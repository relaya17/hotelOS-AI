# Runbook — Kashrut block

**Version:** 1.0 · **Status:** ✅ Approved

## When

Annotation status `block` on procurement/menu/event for a hotel with `kashrut_enabled=1`.

## Response

1. Admin → כשרות → review message
2. Notify F&B via Org Comms channel `kashrut_fb` (seeded)
3. Escalate to GM / owner if unresolved
4. Clear only with human משגיח override (new annotation `ok` + audit)

Never auto-clear blocks via automation.
